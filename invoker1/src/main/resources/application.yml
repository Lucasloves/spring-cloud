server:
  port: 9001

spring:
  application:
    name: service-invoker1

eureka:
  instance:
    hostname: localhost # 服务注册中心实例名
client:
  serviceUrl: # 端口号为8761的服务注册中心地址
    defaultZone: http://localhost:8761/eureka/
